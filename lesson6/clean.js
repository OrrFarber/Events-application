// clean code in js
const currentDate = new Date(); //name of const predicts its purpose

// function calling
const user = {
    getUserName: "Orr", //firstName
    getUserData: [1.2.3], //data
    getUserRecord: "2" //"record"
}

const milisecondsPerDay = 60 * 60 * 24 * 1000; //insted of 86400000
setTimeout(blistoff, milisecondsPerDay) 

locations.forEach(location => { //instead of unreadable
    doStuff(),
    someOtherStuff()
    return(location)
})

// dont add unneded context car { carMake -> Make}

// function arrguments
crateManue({title: "foo", body: "bar"})
createMenu(title:"foo")

//function does one thig : 
function changePassword()
function sendEmail()

//specificty
function addMonthToDate()

//to take info - we create new arrey.
//don't use the current arrey - can affect the code

